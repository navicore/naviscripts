# ~/.tmuxinator/onextent.yml

name: onextent
root: ~/git/navicore

windows:
  - OESCRIPT:
      layout: main-vertical
      panes:
        - cd OeScript && mytags && vim
        - cd OeScript && pwd
        - cd OeScript && pwd
  - GOWORKSPACE:
      layout: main-vertical
      root: ~/go
      panes:
        - vim
        - cd src/github.com/navicore/oescriptgo && pwd
        - cd src/github.com/navicore/oescriptgo && pwd
  - OTHER:
      root: ~
      layout: main-vertical
      panes:
        - cd ~ && vim
        - cd ~ && pwd
        - cd ~ && pwd

